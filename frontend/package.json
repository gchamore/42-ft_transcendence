{
  "name": "front",

  "dependencies": {
    "typescript": "^5.7.3"
  },

  "scripts": {
    "dev": "npx tsc --watch --preserveWatchOutput &",
    "stop-dev": "ps aux | grep -E '\btsc\b' | grep -v 'grep' | awk '{print $2}' | xargs -r kill"
  }
}
