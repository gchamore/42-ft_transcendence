{
  "name": "front",

  "dependencies": {
    "live-server": "^1.2.2",
    "typescript": "^5.7.3"
  },

  "scripts": {
    "tsc": "npx tsc --watch --preserveWatchOutput",
    "live-server": "node start-live-server.js",
    "stop": "pkill -f live-server && pkill -f tsc"
  }
}
